# AGENTS.md

This is a solo-maintainer typesetting template. Preserve deterministic source,
fail closed on compilation or validation errors, and keep publishing tag-only.

Run `make verify` before proposing a change. Never introduce required reviewers,
CODEOWNERS approval, team gates, unpinned actions, shell escape, or automated
publication from an ordinary branch push.
